replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

# Optional: make the controller name predictable
fullnameOverride: sealed-secrets-controller
